<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.goldblastgames.simonsays.tests">
    <application>
        <activity android:name=".SimonSaysActivity" android:label="@string/simonsays">
           <intent-filter>
               <action android:name="android.intent.action.MAIN"/>
               <category android:name="android.intent.category.LAUNCHER"/>
           </intent-filter>
        </activity>

        <uses-library android:name="android.test.runner"/>
    </application>

    <instrumentation android:label="Tests"
                     android:targetPackage="com.goldblastgames.simonsays"
                     android:name="android.test.InstrumentationTestRunner"/>

    <uses-sdk android:minSdkVersion="10"/>
</manifest>
